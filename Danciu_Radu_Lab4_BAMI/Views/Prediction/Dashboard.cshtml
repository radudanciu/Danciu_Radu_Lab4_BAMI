@model DashboardViewModel
@using System.Text.Json

@{
    ViewData["Title"] = "Dashboard predicții";
}

<h2>Dashboard predicții</h2>


<form method="get" asp-action="Dashboard" class="row g-3 mb-4">
    <div class="col-md-3">
        <label class="form-label">De la data</label>
        <input type="date"
               name="fromDate"
               class="form-control"
               value="@(Model.FromDate?.ToString("yyyy-MM-dd") ?? "")" />
    </div>

    <div class="col-md-3">
        <label class="form-label">Până la data</label>
        <input type="date"
               name="toDate"
               class="form-control"
               value="@(Model.ToDate?.ToString("yyyy-MM-dd") ?? "")" />
    </div>

    <div class="col-md-2 align-self-end">
        <button type="submit" class="btn btn-primary w-100">Filtrează</button>
    </div>

    <div class="col-md-2 align-self-end">
        <a class="btn btn-secondary w-100" asp-action="Dashboard">Reset</a>
    </div>
</form>

<div class="row mb-4">

    <div class="col-md-4">
        <div class="card text-bg-primary mb-3">
            <div class="card-body">
                <h5 class="card-title">Număr total de predicții</h5>
                <p class="card-text display-6">@Model.TotalPredictions</p>
            </div>
        </div>
    </div>


    <div class="col-md-8">
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">Preț mediu per tip de plată</h5>

                @if (Model.PaymentTypeStats != null && Model.PaymentTypeStats.Any())
                {
                    <table class="table table-sm mb-0">
                        <thead>
                            <tr>
                                <th>Tip plată</th>
                                <th>Număr predicții</th>
                                <th>Preț mediu</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.PaymentTypeStats)
                            {
                                <tr>
                                    <td>@item.PaymentType</td>
                                    <td>@item.Count</td>
                                    <td>@item.AveragePrice.ToString("0.00")</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <p class="mb-0 text-muted">Nu există date pentru intervalul selectat.</p>
                }
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">Distribuția prețurilor prezise</h5>
                <canvas id="priceChart" height="100"></canvas>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
    
        const labels = @Html.Raw(JsonSerializer.Serialize(Model.PriceBuckets.Select(b => b.Label)));
        const dataValues = @Html.Raw(JsonSerializer.Serialize(Model.PriceBuckets.Select(b => b.Count)));

        const ctx = document.getElementById('priceChart').getContext('2d');
        const priceChart = new Chart(ctx, {
            type: 'bar',
            data: {
                labels: labels,
                datasets: [{
                    label: 'Număr de predicții',
                    data: dataValues,
                    borderWidth: 1
                }]
            },
            options: {
                responsive: true,
                scales: {
                    y: {
                        beginAtZero: true,
                        title: {
                            display: true,
                            text: 'Număr predicții'
                        }
                    },
                    x: {
                        title: {
                            display: true,
                            text: 'Interval preț (unitatea modelului)'
                        }
                    }
                }
            }
        });
    </script>
}
